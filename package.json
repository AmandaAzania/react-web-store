{
  "name": "react-web-store",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",  // specify the version that matches your app's React version
    "react-dom": "^18.0.0",  // same here, ensure it's compatible with the version of React
    "react-scripts": "^5.0.0",  // specific stable version of react-scripts
    "web-vitals": "^4.2.4"  // any other dependencies you have
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
